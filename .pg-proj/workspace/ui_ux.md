# 交互设计（简化使用）

## 入口层

1. 一键快装（推荐）
- `curl -fsSL <host>/install.sh | sh -s -- --fast`

2. 仓库内执行
- `./install.sh --fast`

## fast 模式行为

- 先输出快速检测摘要（OS/权限/网络/关键依赖）
- 默认自动继续安装
- 保留 Windows WSL 推荐确认

## 交互简化原则

- 非关键步骤不询问
- 出错时直接给修复命令
- 成功后输出最短下一步命令

